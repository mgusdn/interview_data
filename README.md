# AI 면접 질문 RAG 시스템

boost-devs/ai-tech-interview 레포를 데이터 소스로 활용한 RAG 시스템입니다.

## 환경 세팅

```bash
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env  # API 키 입력
```

## 실행

```bash
python main.py --query "PCA란?"
```

## 분야

statistics-math / machine-learning / deep-learning / python /
rec-sys / data-structure / operating-system / network
